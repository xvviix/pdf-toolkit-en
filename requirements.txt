# ─────────────────────────────────────────────────────────────
#  PDF Toolkit v12 — core dependencies (light, fast install)
#  Install:  pip install -r requirements.txt
#
#  NOTE: Python 3.10 – 3.13 recommended.
#        Python 3.14+ is NOT supported by the OCR stack yet.
# ─────────────────────────────────────────────────────────────

# Core PDF & images
pypdf>=4.0
PyMuPDF>=1.24
Pillow>=10.0

# ─────────────────────────────────────────────────────────────
#  Optional: OCR (Persian auto name detection)
#  Install separately ONLY if you need it:
#     pip install -r requirements-ocr.txt
#  The app works fine without it (manual naming mode).
# ─────────────────────────────────────────────────────────────
